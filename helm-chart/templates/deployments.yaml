apiVersion: apps/v1
kind: Deployment
metadata:
  name: interview1-app
  labels:
    app: interview1-app
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: interview1-app
  template:
    metadata:
      labels:
        app: interview1-app
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 10001
      containers:
        - name: flask-app
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePipeline: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: 5000
              protocol: TCP
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
          livenessProbe:
            httpGet:
              path: /health
              port: 5000
            initialDelaySeconds: 10
            periodSeconds: 15
          readinessProbe:
            httpGet:
              path: /health
              port: 5000
            initialDelaySeconds: 5
            periodSeconds: 10